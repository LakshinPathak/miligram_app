const express = require('express');
const router = express.Router();
//const User = require('../models/User');
//const Post = require('../models/Post1');

// Define your routes for profiles here

module.exports = router;
